<odoo>
    <record id="view_purchase_order_form_inherit_brand_category" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.brand.category</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">

            <!-- Add fields before product -->
            <xpath expr="//field[@name='order_line']/list/field[@name='product_id']" position="before">
                <field name="category_id" widget="many2one" width="100%"/>
                <field name="brand_id" widget="many2one" width="100%"/>
            </xpath>

            <xpath expr="//field[@name='order_line']/list/field[@name='product_id']" position="attributes">
                <attribute name="domain">
                    [
                        ('purchase_ok', '=', True),
                        '|', ('company_id', '=', False), ('company_id', 'parent_of', parent.company_id),
                        '|', ('categ_id', '=', category_id), ('id', '!=', False)
                    ]
                </attribute>
            </xpath>

        </field>
    </record>
</odoo>